Rows for sidebar catalogs — frameworks, snippets, saved files.

```jsx
<ListRow lead={<Check />} name="Chart.js" tools={<IconButton title="Remove"><Icon name="close" size={13}/></IconButton>} />
<ListRow name="Fluent System Icons" on />
<ListRow name="DCS Standard Include" muted />
```
