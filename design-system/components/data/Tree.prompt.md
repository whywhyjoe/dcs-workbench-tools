The response / console inspector. Renders an object as a foldable mono tree.

```jsx
<Tree data={{ web: { Title: 'New Nerve', ItemCount: 128, Hidden: false } }} />
```

Value colours mirror the `dcspad-dark` Monaco theme on purpose — do not restyle them.
