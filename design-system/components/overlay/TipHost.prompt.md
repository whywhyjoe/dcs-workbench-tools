The one-phrase hover label for controls that are all glyph.

```jsx
<TipHost tip="Maximise pane" below>
  <IconButton title="Maximise pane"><Icon name="maximize" size={13} /></IconButton>
</TipHost>
```

It is not a tooltip system. Keep the `title`/`aria-label` on the control regardless.
