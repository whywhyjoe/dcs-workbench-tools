The persistent path bar under an L1 topbar — where you are on the tenant.

```jsx
<Crumbs items={['/sites/NewNerve', 'Documents', 'Splash']} onNavigate={go} />
```

Server-relative paths, always mono, never abbreviated with an ellipsis in the middle.
