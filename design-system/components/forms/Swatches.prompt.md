A row of curated colour swatches for a generator's output colour.

```jsx
<Swatches colors={['#3fd8b4', '#67a7f7', '#e8b660', '#f2f5fa']} value={c} onChange={setC} />
```

Offer the palette the brand supports; a free picker produces off-system output.
