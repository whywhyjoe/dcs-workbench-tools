Toggle for a setting that applies the moment you flip it; Check for a value submitted with a form.

```jsx
<Toggle label="Auto-run" checked={auto} onChange={e => setAuto(e.target.checked)} />
<Check label="Clear console on run" defaultChecked />
```
