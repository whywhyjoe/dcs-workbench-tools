The edge you drag to resize a preview — used when the pixel value does not matter, only the fit.

```jsx
<ResizeRail dragging={drag} onPointerDown={startDrag} tip="Drag to resize preview" />
```

Arrow keys should nudge when focused. Reach for Slider whenever the number itself matters.
